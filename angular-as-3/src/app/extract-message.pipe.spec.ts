import { ExtractMessagePipe } from './extract-message.pipe';

describe('ExtractMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
